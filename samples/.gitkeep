# Sample files directory
# PDFs will be generated here during testing

# To generate a sample PDF:
# 1. Run npm run seed to create test leads
# 2. Use the calculator at /soc-2-cost-calculator
# 3. Click "Send Me the Full PDF" to generate and email PDF

# Or use the API directly:
# curl -X POST http://localhost:3000/api/generate-pdf \
#   -H "Content-Type: application/json" \
#   -d '{"lead_id": "<your-lead-id>"}'
